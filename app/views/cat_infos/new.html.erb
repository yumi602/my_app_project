<div class="flex-grow flex items-center justify-center">
  <div class="w-full md:w-3/4 lg:w-2/3 max-w-2xl">
    <h2 class="text-3xl font-bold mb-6">入力内容の確認</h2>

    <h3 class="text-xl font-semibold mb-4">猫の基本情報</h3>
    <div class="mb-6">
      <strong>ねこ画像:</strong>
      <% if @form.cat_image.present? && @form.cat_image.respond_to?(:url) %>
        <div class="mt-2">
          <img src="<%= @form.cat_image.url %>" alt="猫の画像" class="rounded-lg shadow-md w-full max-w-xs">
        </div>
      <% else %>
        <p>画像が登録されていません。</p>
      <% end %>
    </div>

    <p class="mb-2"><strong>名前:</strong> <%= @form.name %></p>
    <p class="mb-4"><strong>年齢:</strong> <%= @form.age %></p>

    <h3 class="text-xl font-semibold mb-4">餌の情報</h3>
    <p class="mb-2"><strong>餌の名前:</strong> <%= @form.food %></p>
    <p class="mb-6"><strong>コメント:</strong> <%= @form.body %></p>

    <div class="mb-6">
      <strong>えさ画像:</strong>
      <% if @form.food_image.present? && @form.food_image.respond_to?(:url) %>
        <div class="mt-2">
          <img src="<%= @form.food_image.url %>" alt="えさの画像" class="rounded-lg shadow-md w-full max-w-xs">
        </div>
      <% else %>
        <p>画像が登録されていません。</p>
      <% end %>
    </div>

    <%= form_with url: cat_infos_path, method: :post, local: true do |f| %>
      <div class="flex justify-between mb-4">
        <%= link_to "戻る", new_food_name_path, class: "btn btn-primary w-1/2 mr-2" %>
        <%= f.submit "登録する", class: "btn btn-primary w-1/2" %>
      </div>
    <% end %>
  </div>
</div>